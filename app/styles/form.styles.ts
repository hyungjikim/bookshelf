import * as stylex from "@stylexjs/stylex";

export const formStyles = stylex.create({
  container: {
    maxWidth: "70vw",
    margin: "24px auto",
    border: "1px solid #ccc",
    padding: "24px",
    borderRadius: "12px",
    display: "grid",
    gap: "12px",
  },
});
